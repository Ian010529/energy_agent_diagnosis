# M0 Gate Report

- Acceptance run ID: `019f63f1-f441-70db-aaa0-5e076231fd55`
- Accepted implementation commit: `135e9293175502735b8442abdfc5330c035f2254`
- Result: `ACCEPTED`
- Started: `2026-07-15T04:03:48.929824+00:00`
- Finished: `2026-07-15T04:19:19.670885+00:00`
- Environment: Docker Engine `linux/arm64`, Python `3.12.13`
- Profiles: fresh isolated Compose projects for `full`, `staging`, and `production`

## Commands actually executed

The acceptance run executed `make gate-m0`. Its recorded steps were:

```text
git status --porcelain --untracked-files=all
uv run python -m scripts.prepare_m0_env
make verify-design
make lint
make typecheck
uv run pytest tests/unit --junitxml=artifacts/gates/M0/019f63f1-f441-70db-aaa0-5e076231fd55/unit-junit.xml
uv run pytest tests/contract --junitxml=artifacts/gates/M0/019f63f1-f441-70db-aaa0-5e076231fd55/contract-junit.xml
docker compose --env-file deploy/versions.env --env-file .env.m0 --profile full config --quiet
docker compose --env-file deploy/versions.env --env-file .env.m0 --profile full up -d --wait --wait-timeout 600
M0Probe.readiness through authenticated production protocols
M0Probe.write including RabbitMQ publisher confirm
docker compose ... --profile full restart <each service>; authoritative persistent readback
fresh isolated staging project: up --wait; authenticated readiness; down --volumes
fresh isolated production project: up --wait; authenticated readiness; down --volumes
make verify-design
```

The full Gate restarted, in order: MySQL, Redis, RabbitMQ, MinIO, InfluxDB,
OpenSearch, etcd, Milvus, Neo4j, Keycloak, and Toxiproxy.

## Test results

| Layer | Passed | Failed/errors | Skipped |
|---|---:|---:|---:|
| Unit | 12 | 0 | 0 |
| Contract | 17 | 0 | 0 |
| Total | 29 | 0 | 0 |

`make lint`, `make typecheck`, the initial immutable-design verification, and the
final immutable-design verification passed. The immutable design SHA-256 remained
`c559a530387de5fc1afced506e406967e74c18ed76e659b4b062c2051b615a11`.

## Real-service persistence evidence

The full profile used authenticated production protocols to write data, restart each
service without deleting its volume, and read the final effect from the authoritative
service. Every readback produced the payload SHA-256
`7996f647f89fb14fc8b95971e9cb503821c3c460f9ee944604df12567ace6e77`.

| Service | Real write and authoritative post-restart readback |
|---|---|
| MySQL 8.4.6 | Table row upsert and SQL select |
| Redis 7.4.5 | Authenticated key write/read; AOF settings verified |
| RabbitMQ 4.1.3 | Durable queue/message with publisher confirm and manual read/ack |
| MinIO 2025-04-22 | Object write and object-body checksum |
| InfluxDB 2.7.12 | Point write and Flux query |
| OpenSearch 2.19.3 | Document index and document get |
| etcd 3.5.18 | Key put and `etcdctl` persistent get |
| Milvus 2.5.14 | Authenticated 1024-dimensional vector upsert and query |
| Neo4j 5.26.9 | Authenticated node/edge merge and Cypher readback |
| Keycloak 26.3.2 | Imported client token before mutation; group attribute write/read |
| Toxiproxy 2.12.0 | Redis write/read through the real proxy before and after restart |

All images were configured and observed by immutable digest. The detailed configured
references and runtime image IDs are recorded in the sanitized
`image_digests.json` artifact for this run.

## Profile isolation and readiness

The profile projects were distinct and used fresh volumes:

- `energy-agent-m0-60cfa821b6fb-full-019f63f1`
- `energy-agent-m0-60cfa821b6fb-staging-019f63f1`
- `energy-agent-m0-60cfa821b6fb-production-019f63f1`

After full persistence testing and cleanup, staging and production were started one at
a time and passed protected-profile validation plus authenticated readiness. Keycloak
readiness required a client-credentials token from the realm imported from the
committed realm file; the probe could not create or repair a missing realm/client.

## Fault injection, performance, and tracing

- Fault injection: every full-profile infrastructure container was restarted
  sequentially and its persistent effect was read back successfully.
- Toxiproxy path: the Redis write/read crossed Toxiproxy before and after proxy restart.
- Performance: not applicable to M0; no application load target is defined for this module.
- Trace verification: not applicable to M0; application tracing is introduced later.
- Cleanup: the run left no run-scoped containers, volumes, or networks.

## Independent review

An independent Codex CLI review ran in a read-only sandbox against implementation
commit `135e9293175502735b8442abdfc5330c035f2254` and this acceptance run. It was
explicitly prohibited from running Make, tests, Docker, services, or state-changing
commands.

Result: **no unresolved P0/P1/P2 findings**.

## Blockers

None.
