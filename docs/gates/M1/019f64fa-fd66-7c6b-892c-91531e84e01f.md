# M1 Gate Report

- Acceptance run ID: `019f64fa-fd66-7c6b-892c-91531e84e01f`
- Base main SHA: `3bc15f77229a7fa5ba5ab2b82fa2892ac22773e8`
- Working branch: `codex/m1-shared-contracts-migrations`
- Tested implementation commit: `465ee9fbf5a8397cb4f363229ac8c566f3f23673`
- Evidence commit SHA: `465ee9fbf5a8397cb4f363229ac8c566f3f23673`
- Result: `ACCEPTED`
- Started: `2026-07-15T08:53:18.310885+00:00`
- Finished: `2026-07-15T08:54:09.506414+00:00`
- Environment: isolated Docker Compose project on `linux/arm64`, Python `3.12.13`
- Canonicalization version: `2`

The evidence commit is the clean, source-exact implementation commit against which
the machine-readable Gate artifacts were generated. This documentation and status
finalization does not change runtime, migration, configuration, test, or Gate behavior.

## Commands actually executed

The acceptance run executed `make gate-m1`. Its recorded commands and semantic Gate
actions were:

```text
git status --porcelain --untracked-files=all
git rev-parse HEAD
make verify-design
make lint
make typecheck
uv run pytest tests/unit --junitxml=artifacts/gates/M1/019f64fa-fd66-7c6b-892c-91531e84e01f/unit-junit.xml
uv run pytest tests/contract --junitxml=artifacts/gates/M1/019f64fa-fd66-7c6b-892c-91531e84e01f/contract-junit.xml
docker compose ... up -d --wait profile-guard mysql redis
apply and verify control/ops 0001 from empty databases
idempotently reapply and verify control/ops 0001
SIGKILL each migration child after DDL step 2 and resume
apply mutated temporary migration copies and require checksum refusal
write/read same revision hash and reject different revision hash
execute Redis Lua late-key type error and compare full key snapshots
uv run pytest tests/integration/test_m1_mysql_migrations.py --junitxml=artifacts/gates/M1/019f64fa-fd66-7c6b-892c-91531e84e01f/integration-junit.xml
uv run pytest tests/live/test_m1_redis_atomicity.py --junitxml=artifacts/gates/M1/019f64fa-fd66-7c6b-892c-91531e84e01f/live-junit.xml
docker compose ... restart mysql redis
authoritative MySQL/Redis readback after restart
alter run-scoped schemas and require verify/apply drift refusal
docker compose ... down --volumes --remove-orphans
assert run-scoped containers, volumes, and networks are absent
make verify-design
```

After M1 passed, `make gate-m0` was executed against the same implementation commit.

## Test results

| Layer | Passed | Failed/errors | Skipped |
|---|---:|---:|---:|
| Unit | 29 | 0 | 0 |
| Contract | 41 | 0 | 0 |
| Integration | 2 | 0 | 0 |
| Live | 1 | 0 | 0 |
| Total | 73 | 0 | 0 |

Lint, type checking, and both immutable-design checks passed. The immutable design
SHA-256 remained
`c559a530387de5fc1afced506e406967e74c18ed76e659b4b062c2051b615a11`.

## Real services and schema evidence

| Service | Version | Immutable image digest |
|---|---|---|
| MySQL | 8.4.6 | `sha256:869218921e61d6c3c89820955d63cca42971f0e3e6c1e2792247bbd944ebc6e9` |
| Redis | 7.4.5 | `sha256:bb186d083732f669da90be8b0f975a37812b15e913465bb14d845db72a4e3e08` |

| Schema | Migration-set checksum | Schema manifest hash |
|---|---|---|
| control | `4874c24cb85b986f3cdf624c14f7a33920266901628b93d614d9d40a45e0fa2a` | `eb65743b766078360a4e5f8998158833fa5a470389e630c192547d1838bdea4e` |
| ops | `3632977903bedf2b89bdaa6ad619de932b9a69e94cc1ed1d0c74f42c3e61362b` | `57ba0634db19f49a562d743d5e61c9afed9dc5ff206e01442a321ec7f5cc61a7` |

Both MySQL manifest hashes were unchanged after service restart. The Gate also
verified UTC, `utf8mb4`, `utf8mb4_0900_ai_ci`, table constraints, indexes, foreign
keys, and canonicalization version `2` through the committed descriptors.

## Recovery, idempotency, and negative-path evidence

- The control migration process was terminated with `SIGKILL` return code `137`
  after step 2, resumed to `APPLIED`, and read back all 22 steps.
- The ops migration process was terminated the same way and resumed with all 11
  steps applied.
- Reapplying both complete migration sets was idempotent.
- Mutated migration checksums were rejected for both schemas.
- A missing schema manifest was rejected by the read-only verifier for both schemas;
  only the apply lifecycle could recreate it.
- Deliberate schema drift was rejected by both verify and apply for both schemas.
- The first diagnosis revision write returned `APPLIED`; an identical replay returned
  `ALREADY_APPLIED`; the same revision with a different hash was rejected. The
  authoritative stored payload hash was
  `b891fd0ed8ed90a02b94e81cffce899e9e9021161fd67eebab897950b31b696c`.
- The real Redis Lua probe detected a late key-type mismatch before any write. Full
  before/after snapshots proved zero partial writes. The successful path persisted
  the expected `hash`, `hash`, `zset`, and `stream` key types and survived restart.
- Final cleanup readback found zero run-scoped containers, networks, and volumes.

## M0 regression

`make gate-m0` passed against the same implementation commit with acceptance run ID
`019f64fb-f254-7030-be72-2c6b45f0fca4`: 70 tests passed, 0 failed/errors, and 0
skipped. The full, staging, and production profiles used real authenticated services;
the full profile completed authoritative post-restart persistence readback, and all
run-scoped resources were removed.

## Independent review

A new independent reviewer inspected the full M1 change and both Gate evidence sets
under an enforced read-only boundary. The reviewer did not modify files or Git state,
run Docker or real services, execute a live Gate, or create acceptance evidence.

Result: **PASS — no unresolved P0/P1/P2 findings**. The reviewer confirmed that the
M1 and M0 evidence remained source-exact for the tested implementation commit.

## Blockers and secret handling

Unresolved blockers: none.

The committed report and sanitized Gate evidence contain no credentials or secret
values. Runtime secret configuration stores only environment key names and approved,
redacted secret references.
