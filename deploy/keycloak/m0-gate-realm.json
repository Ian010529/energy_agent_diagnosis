{
  "realm": "m0-gate",
  "enabled": true,
  "clients": [
    {
      "clientId": "${KEYCLOAK_M0_CLIENT_ID}",
      "enabled": true,
      "publicClient": false,
      "serviceAccountsEnabled": true,
      "standardFlowEnabled": false,
      "secret": "${KEYCLOAK_M0_CLIENT_SECRET}"
    }
  ]
}
