# Energy Equipment Operations Diagnosis Agent

This greenfield repository is built module by module under the gates in
`docs/development/CODEX_EXECUTION_SPEC.md`. M0 contains only repository and
real-infrastructure foundations; it contains no diagnosis Agent business code.

The detailed design under `docs/immutable/` is immutable. Verify it before and
after each module:

```bash
make verify-design
```
